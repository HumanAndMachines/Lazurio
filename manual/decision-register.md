# Registr rozhodnutí sdíleného frameworku

Dokumenty Lazurio rootu odkazují na rozhodnutí číslem (`decision NNNN`).
Číslo je stabilní identifikátor; tento registr je **lokální projekce norem
uvnitř Lazuria** — uživatel Lazuria nepotřebuje žádný externí
repozitář. Plné decision records (kontext, founder verbatim, historie) drží
privátní strategické repo maintainerů frameworku; pro práci v Lazuriu
jsou závazné texty tohoto repa: `AGENTS.md`, manuály, skilly a tento registr.
Mezery v číselné řadě jsou normální (rozhodnutí mimo scope sdíleného
frameworku se sem nepřenášejí).

| # | Norma (shrnutí) |
| --- | --- |
| 0013 | Launchpad root je workstation/control-plane pattern: právě jeden root na mašinu; Organizace dynamicky načítá a spouští, ale nedrží jejich business pravdu. |
| 0018 | Každá Organizace má vlastní doctor; diagnostika je per-Organizace, ne globální. |
| 0021 | Team je pojmenovaná skupina uvnitř Organizace; hosted vzor aplikací je `<modul>.<team>.<doména>`. |
| 0023 | Team může být tým lidí i značka/venture; příslušnost modulů deklaruje manifest. |
| 0024 | Historický CEO-first koncept Launchpadu; revidováno decision 0047 (builder-first). |
| 0026 | Kanonický layout Organizace GEN3 používá `lazurio.organization.json` jako jedinou authoring autoritu Organization manifestu, plochý `workspace/` a samostatný `modules.manifest.json`; `company.gen3.json` je během kompatibilitního okna pouze deterministická legacy compatibility projection. |
| 0030 | Lazurio root je direct-pull klon jediného sdíleného upstreamu; vylepšení jdou zpět PR-em, ne fork-syncem. |
| 0031 | Org mounty `organizations/<org>/` jsou gitignored Doctor-managed vnořená repa, ne git submoduly; root config je folder-driven a každý mount se klasifikuje právě jedním normalizovaným Lazurio Core výsledkem, ne samostatnou volbou filename. |
| 0033 | Migrace GEN2 → GEN3 je fork-based a paralelní; stará generace zůstává rollback linkou. |
| 0034 | Mission Control ↔ template roadmap loop: plánovací vrstva se propaguje template cestou. |
| 0035 | Datové v3 aplikace rozlišují Draft a Publikaci dat nad repository-db; chráněné cesty jdou flow draft → approve → publish. Rozsah approval sleduje progresivní zamykání (viz 0102). |
| 0036 | Mission Control cutover z legacy YAML má explicitní gates; historické záznamy se nepřepisují. |
| 0037 | Mission Control v3 nastupuje na hranici GEN3 migrace Organizace. |
| 0039 | Historické produktové názvosloví; uživatelskou komunikaci a jméno sdíleného systému superseduje decision 0128. |
| 0040 | Pyramida přednosti source of truth: decision records > schémata/configy > GLOSSARY > AGENTS.md scope > kontrakty > Guide. |
| 0041 | (1) `workspace/` je plochá složka všech modulů; (2–5) Team je manifestová N:M deklarace, ne adresář — chybějící deklarace = default Team `workspace`; (6) `productionspace` je rezervovaný org-level slug mimo Teamy; (7) každé productionspace repo má vlastní branch/release pravidla a doctor vynucuje jen bezpečné minimum. |
| 0042 | Launchpad je auto-discovery first: skenuje kandidátní Organization mounty a jejich `lazurio.organization.json` / legacy compatibility projection vyhodnotí jediný Core resolver s one-resource-per-mount deduplikací a fail-closed konfliktem; root config není allowlist a bezpečnostní kontroly platí pro všechny mounty stejně. |
| 0043 | Neplatný manifest Organizaci izoluje: Launchpad vadný mount bezpečně odstaví, nikdy kvůli němu nepadá celé UI. |
| 0044 | Noví klienti nastupují rovnou na GEN3 (žádný GEN2 onboarding). |
| 0045 | `_GENn` je trvalý generační marker názvu repa/mountu; interní brand identita zůstává čistá. |
| 0046 | Gbrain (paměť Buddyho) patří do personalspace, nikdy do firemní organizace. |
| 0047 | Dvě surfaces: Launchpad = builder-first lokální; Lazurio Dashboard = hosted admin/user vstup. |
| 0048 | Produktové plány Free/Solo/Team/Enterprise a hosting režimy (localhost/hosted/selfhosted). |
| 0049 | Worktree runtime kontrakt: plan-owned worktrees v `.worktrees/`, sidecar metadata, Launchpad spouští aplikace z worktrees. |
| 0051 | Struktura Personalspace: privátní repo `<login>/<login>_GEN3` mimo firemní organizace, `personal.gen3.json`, plochá `workspace/`, gbrain jako root vrstva. Organization manifest amendment 0026/0031/0042 tento Personalspace filename ani jeho soukromý rollout nemění. |
| 0052 | AI Architekt je placená platformní služba admin vrstvy: navrhuje změny výhradně přes Dashboard apply plány se schválením Organization Admin, nikdy nezapisuje přímo do zákaznického Gitu a nenahrazuje BYOS builder agenty. |
| 0059 | Historický root-only kanálový update; pracovní checkout pravidla superseduje decision 0129. Pravomoc vytvořit Release dál určují živá GitHub práva. |
| 0060 | Role určuje footprint na mašině: Organization User je zero-install (žádný lokální root, přístup přes produkční aplikace a MCP). |
| 0061 | BYOS: agentní runtime a subscription zůstávají na stroji buildera; platforma nedodává LLM účet ani skrytou autonomii. |
| 0062 | Kanonická čtveřice person: Organization Admin / Steward / Builder / User (nesklonné anglické pojmy). |
| 0063 | Worker Agent pracuje jen v explicitně autorizovaném tasku pod dozorem; drafty schvaluje persona s pravomocí (Kolega nebo AI Kolega — gate je pravomoc, ne rozdíl člověk vs. AI). |
| 0077 | OrganizationTemplate rename a template-first flow; template identitu určuje validovaný `organization_kind` marker, runtime ji vyloučí a neodvozuje ji z názvu. Původní top-level mount lokaci superseduje 0127. |
| 0079 | Personalspace self-service vzniká z veřejného `PersonalspaceTemplate_GEN3`; reálná instance je vždy privátní repo vlastníka. |
| 0080 | Buddy runtime běží výhradně na dedikované VPS vlastníka; localhost není instalační volba ani fallback. |
| 0089 | Buddy je důvěryhodný osobní zástupce lidského Principála: morální kontrakt (`CONSTITUTION.md`) + trvalé, scoped, odvolatelné mandáty (`MANDATES.md`); transakčně specifické gates mandát nikdy nenahrazuje a Buddy si mandát sám nevydá. |
| 0090 | Slovník person: Worker Agent je kanonický pojem pro execution session bez pravomocí; „Agent" je hovorová zkratka. |
| 0091 | Security hranice: Personalspace patří výhradně jednomu Principálovi (+ volitelný Buddy); Principál plně ovládá svou mašinu; GitHub je jediná autorita Workspace source přístupů; repo modulu je nejmenší access hranice. |
| 0092 | AI Kolega má vlastní GitHub účet, dedikovanou Mašinu (GEN2/GEN3 = VPS), vlastní kompletní lokální instalaci Lazuria a owner-only Personalspace bez Buddyho; do každé Organizace smí jen to, co dovolí jeho vlastní GitHub identita. |
| 0093 | Infra repo Organizace je Admin-only: Steward ani Builder do něj grant nedostávají. |
| 0094 | Opatrovník: každý seat AI Kolegy má právě jednoho jmenovaného lidského custodiana s auditovaným, jmenovitým servisním vstupem — jiná osa než organizační role; soukromý Personalspace Kolegy se nečte. |
| 0095 | Admin smí mergovat i vlastní PR; Steward je běžná merge lane, ne výhradní autorita. |
| 0102 | Lokální Mission Control writer používá GitHub identitu přihlášeného Principála (žádný druhý IAM); datová lane se zamyká progresivně. |
| 0103 | Agentní PR disciplína: vždy worktree + PR, průběžný push, Draft PR → Ready, průvodcovský handoff, Publikace řízená živými GitHub právy, progresivní zamykání `main`. Předání jinému Principálovi používá reviewer request pro kontrolu a assignee pro vlastnictví dotažení; Task Agent pod přiřazeným GitHub účtem pracuje přímo na PR branchi včetně bezpečného `--force-with-lease` a nevrací změny autorovi pouhým komentářem. Assignment sám neuděluje GitHub práva ani publikační mandát. |
| 0104 | `.claude/skills` je Git-tracked byte-for-byte mirror `.agents/skills` (Windows-safe, žádné symlinky); paritu hlídá doctor a opravuje repair lane. |
| 0112 | Agentní instrukce jsou ústava: vysvětlují hodnoty, hranice a očekávání, nediktují postup; slovník pěti pojmů (Principál, Kolega, AI Kolega, Worker Agent, Buddy); jedno pravidlo = jeden kanonický domov; mechaniku nese skript/skill/doctor. |
| 0113 | Přejmenovatelné jméno (slug, label, deklarace) není autorizační ani join klíč: vazby a výběr drží stabilní identita a ověřený stav, nikdy samotné jméno. |
| 0118 | Composable doctor surface: root doctor svolává vlastní doctory namountovaných rep podle deklarace v manifestu, agreguje vnořené reporty a rozbitého potomka hlásí nahlas; slovník stavů `not_applicable` / `blocked` / `incomplete`. |
| 0124 | Document-native Workspace modul používá pro Markdown/MDX jedinou write/publish cestu Git branch + PR; druhý writer ve v2 nevzniká a oddělený v3 authoring profil vyžaduje samostatně doložený a schválený use case. |
| 0125 | Stabilní identita modulu a fyzický repository mount jsou oddělené osy: `slug` je explicitní lowercase kebab-case ID pro vazby a UI, zatímco basename `workspace/<repo-name>` nebo `productionspace/<repo-name>` zachovává přesný název GitHub repozitáře včetně case, `_` a `.`. Case-preserving basename bez bezpečně odvoditelného nebo explicitního slugu failuje zavřeně. |
| 0127 | Pracovní Personalspace/Organization template checkouty žijí jako org-level nested repa v `organizations/<AdminOrganization>/productionspace/`; bez Team membership, aliasu, provider transferu nebo visibility změny. |
| 0128 | Sdílený systém, framework, root, Launchpad hlášky, návody a další současné uživatelské povrchy se jmenují **Lazurio**. Starší názvy zůstávají pouze v historických auditních záznamech. Přejmenování nemění právní jméno HumanAndMachine s.r.o., skutečné GitHub/Organization identity, existující filesystem cesty ani kompatibilní CLI, API a datové identifikátory. |
| 0129 | Jediný obecný update je explicitní `lazurio update`: sekvenčně Lazurio Root → Organization Rooty → čerstvě rediscoverovaná spravovaná org-level repa a Workspace Moduly, vždy clean `main` a ff-only. Chybějící Workspace Modul se atomicky materializuje z úplných Git souřadnic; chybějící root repo pouze s explicitním `materialization: doctor_managed_nested_repo`. Dirty tracked/untracked práce jde do ověřeného neobnovovaného recovery stashe; wrong branch se vrací na `main` se zachováním commitů. Ahead/diverged main, nebezpečný detached stav a probíhající Git operace jsou blocked s Codex promptem. Jeden nevalidní slot je karanténa, ne globální `inventory_unavailable`: zdraví sourozenci pokračují a rename/transfer uvnitř jedné Lazurio Organization řeší Agent přes guarded check + exact-fingerprint apply, který zachová Git data a nevytvoří duplicitní clone. Cross-Organization přesun je explicitní source/access migrace a bez budoucí neměnné migrační identity se z Organization-scoped slugu ani remote redirectu nehádá. Root repo bez opt-inu, Productionspace, Personalspace, worktrees a root-space repository-db jsou vyloučené. CLI, Launchpad Sync a legacy adaptéry volají tentýž engine; první render je GET-only bez fetch. |
| 0130 | Kanonický aktivní název nástrojové pracovní relace je **Task Agent**, hovorově **Agent**; strojový enum je `task_agent` bez aktivního kompatibilního aliasu. Decision superseduje pouze terminologii starších záznamů 0063, 0090 a 0112, jejich historické znění ani auditní provenienci nepřepisuje. |
| 0131 | Worktree Task Agenta nese gitignored lokální vodítko k původní relaci pro obnovu rozdělané práce. Je pouze orientační a nezakládá identitu, atribuci ani oprávnění; přesný interní kontrakt drží `manual/worktree-management.md`. |
| 0132 | Principál vlastní chtěný výsledek, priority a Publikaci; Task Agent nese odbornou odpovědnost za elegantní a čisté řešení v návrhu a Draftu. Každá source změna používá risk-proportional `architecture-shaping`: malý zásah dostane rychlou kontrolu, zatímco nová dlouhodobá abstrakce, stav, autorita, hranice, rozhraní, závislost nebo migrace vyžaduje porovnání variant, nejmenší úplný řez, failure/rollback scénáře a důkaz na consumerovi. Kontrakt nepřiděluje Agentovi business ani přístupovou pravomoc a není závislý na konkrétním reviewerovi, modelu nebo CLI. |
| 0133 | Dependency autoritu Workspace App tvoří její manifestem deklarovaný package root a verzovaný lockfile, nikdy filesystem mtime, lokální `.env` ani předchozí `node_modules`. Relativní `file:` smí read-only odkazovat na přesný kanonický package cíl uvnitř stejné Organizace; mutation/ancestor hranicí zůstává owning checkout a jiný symlink cíl, cross-Organization přesah nebo změna cílové package identity failuje zavřeně. Změna repozitáře, který lokální cíl vlastní, zahrne do post-update obnovy i jeho jinak nezměněné App consumery. `Install` zachová existující strom; `Repair` odstraní přesný odvozený `node_modules` a provede frozen instalaci bez druhé recovery autority. Selhání odstraní částečný strom, izoluje App a další Repair začíná znovu čistě. Po skutečné změně source tentýž mechanismus automaticky ověří dotčené Apps a při prvním selhání jednou zkusí čistou opravu. Životní cyklus běžící managed aplikace vlastní Launchpad a obnoví jej pouze po ověřeném úspěchu; neplatný lockfile zůstává source chybou pro Agenta. |
| 0134 | Přesnou otestovanou verzi Bunu pro localhost Lazurio source, CLI a CI vlastní jediné `package.json#packageManager`. Install Core a Doctor porovnávají exact běžící verzi a nesoulad hlásí fail-closed; externí instalace nebo upgrade zůstává explicitně odsouhlasený krok Agenta/instalátoru, nikdy skrytá součást `lazurio update`. Profilové immutable Resident/Buddy toolchain piny mají vlastní artefaktový lifecycle a tato localhost autorita je nepřepisuje. |
| 0135 | Aktuálnost vývojových nástrojů je explicitní read-only troubleshooting lane `lazurio doctor --tool-updates`, ne automatický updater ani součást běžného Doctoru. Git, GitHub CLI a Codex musí být dostupné v `PATH`; Claude Code je volitelný a kontroluje se jen na mašinách, kde dostupný je. Nástroje se porovnávají s oficiálním stabilním release zdrojem; update, instalaci nebo změnu `PATH` smí Agent provést až po souhlasu Principála s přesnou externí změnou. Neověřitelná aktuálnost se nehádá a zůstává warning. Bun dál vlastní exact Lazurio pin podle decision 0134, nikoli obecný latest release. |
| 0136 | Localhost Lazurio Root leží přímo v OS home. Fresh a Managed target je vždy `<home>/Lazurio` (`~/Lazurio` na macOS/Linuxu, `%USERPROFILE%\Lazurio` na Windows). Lazurio podporuje dva profily: dnešní `source`, kde je ověřený existující home checkout pracovním Rootem a smí do migrace zachovat historický název složky, a budoucí `managed`, kde je canonical target generovaný non-Git Root a volitelný source checkout patří do `<home>/Lazurio/development/Lazurio`. Package/source je oddělená provenance spuštěného CLI. Top-level installer nevytváří picker ani druhý locator; Source → Managed migrace smí začít jen explicitní volbou Principála po complete readiness gate, se zachováním mountů/Git/worktrees/stashů a rollbackem. Generátor nevytváří druhou runtime kopii a ruční přesun není podporovaný postup. Source Root mimo přímého potomka home není podporovaná varianta a případná budoucí změna této hranice vyžaduje nové vědomé rozhodnutí, ne rozšíření detekce jako opravu. |
| 0137 | Launchpad je dílna se třemi lifecycle profily. Localhost spouští exact `main` nebo worktree pouze jako session child aktuální Server instance, graceful shutdown ukončí celý managed process tree a žádné poslední kliknutí se po restartu neobnovuje. Hosted část aktualizována v DEV-6579: T3 a Launchpad jsou always-on, výchozí Apps odvozené z Organization manifestů se spouštějí až při Open nebo autentizovaném přímém odkazu. Stop je zastaví do dalšího otevření; health ani katalog je nespouští. Spuštěné Apps se udržují izolovaně v aktuální session, první otevření po cold startu používá `main`; worktree volba žije jen v aktuální session a ingress ji zachovává. Produkce přijímá samostatný reprodukovatelný Build a nepoužívá Launchpad ani worktrees. Click-derived desired evidence, service catalog ani jeho controller nejsou autoritou žádného profilu. |
| 0138 | Explicitní `lazurio organization install` je jediná bounded bootstrap autorita pro aktivní root-space `mission-control/db` deklarovaný jako `repository_db_mount`: až po materializaci právě jednoho aktivního installer-managed parent Git repozitáře smí z Organization-owned remote a přesné branche atomicky doplnit chybějící checkout. Je-li deklarovaná Mission Control app/data hranice neúplná, neaktivní nebo pod jiným materializačním kontraktem, install fail-closed skončí `blocked`; nesmí hlásit konvergenci bez dat. Ignore se dokazuje v parent repozitáři, ne jen v Organization rootu; existující checkout musí být čistý a odpovídat exact remote/branch. Installer jej nefetchuje ani nefast-forwarduje a nezískává commit/publish oprávnění; obecný `lazurio update`, worktrees, Productionspace a jiné repository-db mounty zůstávají beze změny mimo tuto autoritu. |
| 0139 | GitHub Issues přesného owning repa jsou jedinou autoritou otevřených technických problémů a nejistot; visibility a živá GitHub práva zachovávají repo a Organization access hranici. Mission Control dál vlastní plán, prioritu, odpovědnost a stav realizace. Vytvoření issue nebo komentáře je Publikace, proto vyžaduje explicitní mandát Principála, kontrolu duplicit a sanitizaci; bez bezpečného cíle vznikne jen handoff draft. Legacy `ISSUES.open.json` a `ISSUES.resolved.json` jsou zmrazený jednorázový migrační vstup bez synchronizace nebo nové autority. |
| 0140 | Onboarding nové pracovní Mašiny má před Organization materializací jediný top-level toolchain gate. Git, GitHub CLI, Codex CLI, Node a přesně pinovaný Bun nejsou připravené pouhou přítomností binárky ani úspěchem ve zděděné instalační relaci: jejich příkazy musí být dostupné v uživatelském `PATH` nového čistého procesu. Install Core váže Bun, Git a GitHub CLI na skutečně ověřený executable a GitHub stav nevydá za hotový bez `git_protocol=ssh`; běžný Doctor spouští Bun i `bun x`, Git, GitHub CLI, GitHub auth s SSH protokolem, Node a Codex a tool-update lane failuje na chybějící či nečitelném povinném nástroji. Instalační Agent smí oficiální instalaci a nejmenší uživatelskou `PATH` změnu provést jen s explicitním mandátem pro přesné nástroje, zachová existující položky a nezasahuje do system-wide `PATH`, package manageru ani cizích verzí bez dalšího souhlasu. Nový GitHub účet páruje jednou přes `gh auth login --git-protocol ssh`, ale hotový SSH transport dokazuje až exact `git ls-remote`. `lazurio organization install` materializuje Organizaci až po gate a samo systémové nástroje ani `PATH` nevlastní. |
| 0141 | Builder readiness je explicitní read-only provider gate `lazurio organization install <github-login> --role builder`: GitHub zůstává jedinou access autoritou a manifest pouze mapuje interní Team na ověřené neměnné GitHub Team ID a asserted slug. Gate používá čerstvý přihlášený účet a samostatně dokazuje aktivní Organization membership, Team membership, efektivní WRITE nebo vyšší oprávnění i Teamový WRITE nebo vyšší grant na Organization rootu a všech aktivních repozitářích určených Builderovi. `planned_slot`, restricted/Admin-only repa a role-mismatched sloty Buildera neblokují. Neúplná vazba nebo READ failuje zavřeně před materializací s přesným účtem, Teamem a repem; gate nic na GitHubu nemění a běžný Doctor zůstává síťově nezávislý. |
| 0142 | Launchpad-owned UI je offline vícejazyčný surface s párovými `cs`/`en` katalogy a stabilními významovými klíči. Locale se vyhodnotí `explicitní preference v prohlížeči → podporovaný browser locale → cs fallback`; přepnutí reloadne stejnou URL bez změny scope a runtime. In-shell Guide používá tutéž autoritu bez druhého switcheru: statická copy žije v katalozích, dlouhý Organization install runbook v explicitně vybraném páru `manual/organization-install.md` / `distribution/locales/en/manual/organization-install.md`; chybějící nebo neshodný zdroj nemá cross-locale fallback. Core/API drží locale-neutral reason kódy a parametry, chování nikdy nevětví podle přeložené věty. Organization-owned názvy, popisy, commit text a technická evidence se nepřekládají. Budoucí Managed Root se skládá z immutable source jako `resident profile × locale` pouze pro Root-owned generované instrukce včetně `AGENTS.md`; namountované Organization repozitáře zůstávají byte-for-byte pod jazykovou autoritou Organizace. |
| 0143 | Restricted (Admin-only) scope Organization slotu se odvozuje výhradně z jeho deklarace `default_access: restricted`/`private`, nikdy z názvu nebo cesty; `expected`, `optional` a `role_based` jsou běžné sloty a neznámý či malformed režim je fail-safe `access_classification_unknown` bez clone i provider čtení. Běžný `lazurio update` absentní restricted slot nikdy automaticky nematerializuje (`restricted_not_materialized`) a už namountované restricted checkouty dál aktualizuje. Jediný opt-in pro absentní restricted slot je explicitní Admin `lazurio organization install <login>` bez `--role`; `--role builder` i nová `--role steward` materializují jen běžné sloty a jejich descendants včetně povinného Mission Control `repository_db_mount` a restricted slot i vše pod ním vrátí jako záměrné `excluded_by_role_scope` bez jediné provider operace, odlišené od chybějícího grantu. Steward gate je stejný read-only membership/WRITE gate jako Builder nad běžnými sloty své role. Doctor hlásí záměrně absentní restricted slot jen advisory a dál failuje na chybějící povinné aplikaci nebo datovém mountu. Persistentní workstation ACL ani druhý manifest nevzniká. |
| 0144 | Hranice mezi Machines a LazurioPlatform je handover jedné Mašiny. Machines dodají Mašinu online: systém, síť, SSH a sudo, identitu Mašiny v `lazurio.machine.json`, vstup přes společnou Auth bránu a nainstalovaný release LazurioPlatform. Vše uvnitř Mašiny — Lazurio Folder, Organizace, Moduly, nástroje, přihlášení a jejich aktualizace — vlastní LazurioPlatform přes `lazurio` CLI a operátor Mašiny; Machines neudržují vlastní resident ani artefakty a konvergují mazáním, ne přepisem. Soubor je owner-bound popis identity, ne registr, IAM ani manifest s oprávněními: zužuje starší pravidlo „žádný nový manifest“ v `ARCHITECTURE.md` právě na tento handover kontrakt a nic neautorizuje. Slovník: **Conglomerate** je pohled na graf všech Mašin Principála napříč jeho Organizacemi (žádná autorita, ACL ani registr; jako název produktu zůstává deprecated podle 0128, jako odborný pojem je platný); **Conglomerate Host** je Mašina, která pro jeden Conglomerate provozuje Headscale a Vaultwarden; **Lazurio Environment** je prostředí jedné Mašiny složené z vybraného profilu, Lazurio Folderu a všech dostupných nástrojů a přihlášení, ve kterém pracují Agenti. |
| 0145 | Kompatibilitní okno Organization manifestu má konec: `company.gen3.json` je od tohoto rozhodnutí deprecated a cílový stav každé Organizace je pouze `lazurio.organization.json`, bez legacy projekce. Potvrzuje 0026/0031/0042 (`lazurio.organization.json` je jediná authoring autorita, `company.gen3.json` je během okna jen deterministická legacy compatibility projection čtená jedním Core resolverem) a doplňuje mandát dokončit migraci ve všech Organizacích. Přechod se řídí per-root stavy z `manual/lazurio-manifest-family.md`: `legacy` (jen `company.gen3.json`) → `transition` (oba soubory s paritou; canonical je `lazurio.organization.json`, legacy je generovaná projekce, nikdy druhá autorita) → `current` (jen `lazurio.organization.json`). Odstranění `company.gen3.json` v dané Organizaci je dovoleno až po finalizační bráně (`lazurio migrate organization-manifest --finalize`), tj. až všichni mutation-capable čtenáři a podporované Machines projdou reader/update gate; do té doby root v `transition` legacy projekci regeneruje, nemaže. Schéma a migrátor vlastní Lazurio Core podle téhož dokumentu; LazurioPlatform a Machines konzumují tentýž kontrakt (vendorovaný s provenance), nevzniká druhé schéma ani druhý zdroj pravdy. |
| 0146 | **Jména a vstup hostovaných Mašin (dílen).** Tři vrstvy jmen pod Organizací: (1) dílna = hostovaná workspace VM: každá aplikace na kořeni vlastního hostname `<app>.<vm>.<org>.lazurio.io`, dostupné výhradně z tailnetu; (2) produkční workspace aplikace pro lidi z Organizace: `<app>.<org>.lazurio.io` přes veřejný wildcard záznam `*.<org>.lazurio.io` v Cloudflare, bez tailnetu, s přihlášením přes Lazurio Auth a klientem registrovaným na Conglomerate Hostu; (3) productionspace repozitáře (monorepo apod.) jsou mimo tento mechanismus. Žádná aplikace se neohýbá pro cestu za lomítkem; bare hostname VM `<vm>.<org>.lazurio.io` je rozcestník (`/`, `/launchpad[/…]` → `https://launchpad.<vm>…/…`, `/<app>[/cesta?query]` → `https://<app>.<vm>…/cesta?query` 302, `/oauth2/*` zůstává), takže lidem se dál sdílí odkaz s lomítkem. **DNS:** zónu `<org>.lazurio.io` obsluhuje na tailnetu Conglomerate Host (decision 0144, důvěryhodný uzel pro workspace funkcionalitu Organizací, které spravuje) přes Headscale split DNS jedním pravidlem per Organizace: `<vm>.<org>` a `*.<vm>.<org>` → MagicDNS jméno VM; resolver Conglomerate Hostu odpovídá jen za labely VM z registru a všechna ostatní jména zóny (produkční aplikace `<app>.<org>`, `auth`, `headscale` …) předává veřejnému DNS, takže produkční jména se na tailnetu překládají stejně jako mimo něj. Veřejný wildcard nutně překládá i `<vm>.<org>` a `<app>.<vm>.<org>` mimo tailnet; tam končí na produkční bráně Organizace, která je fail-closed: obsluhuje jen labely produkčních aplikací z registru, jakýkoli jiný hostname (včetně labelů VM a jejich subdomén) odmítne (421) a nikdy neproxuje do tailnetu ani na VM. Dílna je tedy dosažitelná jen z tailnetu, i když její jméno veřejně existuje. Žádný záznam per VM ani per aplikace; vznik, migrace ani přeinstalace VM DNS nemění. Conglomerate Host je resolver a registr labelů, ne katalog aplikací, autorizace ani autorita klíčů aplikací. **Registr labelů:** VM a produkční aplikace sdílejí namespace `<label>.<org>`; Conglomerate Host přiděluje oba druhy a kolizi odmítá (fail-closed). Jediný kanonický vstup registru je Deployment Repo Organizace (`infra`): labely VM deklaruje roster hostovaných guestů (jméno VM), labely produkčních aplikací roster produkční brány; registr se z merged stavu `infra` rekonciluje stejným mechanismem jako dnešní Headscale DNS reconciler a žádný jiný vstup (ruční, z VM, z Launchpadu) nemá. Registr drží jen label, druh (VM nebo produkční aplikace) a cíl (MagicDNS jméno VM nebo produkční brána), nikdy aplikace uvnitř VM — ty zůstávají v katalogu VM. Lifecycle: přejmenování = uvolnění starého a přidělení nového labelu reviewovanou mutací; přeinstalace VM pod stejným jménem label zachová; po odstranění z rosteru je label 7 dní tombstone (nelze ho vázat na jiný cíl, aby vypršely cache, certifikáty a session) a pak volný. Rezervované labely nejsou ruční seznam: registr odmítne každý label, který má v zóně `<org>.lazurio.io` vlastní veřejný záznam (dnes např. `auth`, `headscale`, `vaultwarden`, hosty Organizace a Conglomerate Host), plus pevné labely brány `oauth2`, `api`, `well-known` a `launchpad` (jen Launchpad). **Pravidla jmen:** label = `[a-z0-9]([a-z0-9-]*[a-z0-9])?`, VM ≤ 32 znaků, aplikace ≤ 63 znaků; katalog normalizuje id modulu, worktree nebo dev serveru na label (lowercase, `_`, `.` a jiné znaky → `-`, sloučení a ořez pomlček, zkrácení na 63), kolize po normalizaci se odmítne (první deklarace vítězí, odmítnuté se hlásí); rezervované labely podle registru výše. Neznámý hostname aplikace brána VM odmítne z vlastního deklarovaného katalogu, nikdy nepropadne wildcardem. **TLS:** každá VM má vlastní certifikát pro `<vm>.<org>` a `*.<vm>.<org>` (Let's Encrypt, DNS-01); privátní klíč vzniká a zůstává jen na VM, Conglomerate Host pouze dokončí DNS-01 challenge pro jména té VM (jediný token k DNS zóně je na Conglomerate Hostu, TXT záznam je dočasný) a certifikát se obnovuje automaticky; žádná VM nedrží wildcard klíč Organizace; hranice vydání a revokace je Conglomerate Host + Auth klient VM. **Přihlášení:** session per hranice: přihlašovací tok `/oauth2/*` s callbackem per VM na bare hostname, cookie `__Secure-lazurio-workspace` s doménou `.<vm>.<org>.lazurio.io` (Secure, HttpOnly, SameSite=Lax) platná pro bare hostname a všechny `<app>.<vm>…`; hostname aplikace ověřuje session forward-auth proti oauth2-proxy své VM; nepřihlášený přímý požadavek na aplikaci → 302 na `https://<vm>.<org>/oauth2/start?rd=<url aplikace>` (rd omezené na `.<vm>.<org>`), po přihlášení návrat na aplikaci; session se nikdy nerozšiřuje na jinou VM ani produkční aplikaci; SSO napříč hranicemi zajišťuje výhradně session identity providera (Keycloak). Katalog aplikací na VM vlastní operátor a plní ho Launchpad; přidání aplikace, worktree nebo dev serveru nevyžaduje rollout Machines. Instalace a rollout Mašin jsou standardizované napříč Organizacemi jednou stavebnicí. Nahrazuje model „jedna origin, aplikace za prefixem“ z operátorského kontraktu Machines (#126) i hosted vzor `<modul>.<team>.<doména>` z decisions 0021/0041 (hostovaná Mašina Teamu je VM se svým labelem); starší odkazy s prefixem zůstávají funkční přes rozcestník. |
| 0147 | **Identita Team Workspace.** Hosted Team Workspace (sdílená Mašina vlastněná Organizací bez přiřazeného operátora) nemá lidskou GitHub identitu. Jeho GitHub identitou je platformní GitHub App **Lazurio for GitHub** (app id 4568928, bot `lazurio-for-github[bot]`) dosahovaná přes per-Organization token broker (`Lazurio/github-app`), který vydává krátkodobé tokeny scoped na jeden repozitář; privátní klíč App nikdy neleží na workspace Mašině — jen v brokeru a custody operátora. Nasazený broker dnes autorizuje výhradně podle id workspace, jeho unikátního credentialu a `repository_ids`; pole `github_team_id` v policy neparsuje ani nepoužívá. **Cílový kontrakt** — právě jeden workspace per Team vázaný neměnným `github_team_id`, jeden broker credential per Team a živé ověření GitHub Team → repository grantu při každém vydání tokenu (fail closed) — vyžaduje versionovanou změnu `Lazurio/github-app` s testy a migračním/readback gate existujících policy ještě před rolloutem; není to existující chování. Zamítnuté varianty: jedna GitHub App per Team (škáluje jako Teams × Organizace, flotila Apps) a teamový GitHub uživatelský účet (druhý IAM mimo „GitHub je jediná autorita přístupů“, lidský credential na sdílené Mašině, placený seat). |
| 0148 | **Atribuce Teamu v Gitu.** Committer je `lazurio-for-github[bot]` (`316166326+lazurio-for-github[bot]@users.noreply.github.com`); author je Teamová pseudo-identita `"<Organizace> Team <slug>" <team.<slug>@<doména Organizace>>` bez vazby na GitHub účet, nastavená v `git config --system` Team Workspace; každý commit nese trailer `Lazurio-Workspace: <organization-slug>/<team-slug>`. Každá změna z Team Workspace jde přes pull request otevřený botem s labelem `team:<slug>` a hlavičkou popisu „Navrhuje Team <slug> z Team Workspace“; reviewuje a merguje oprávněný Operátor (člověk s právy), který tím přebírá odpovědnost. Instalační token s `contents: write` umí na GitHub Free s nechráněnou `main` technicky pushnout přímo do `main` i splnit merge endpoint pull requestu; pravidlo „jen přes PR, merguje oprávněný Operátor“ proto na Free drží proces a policy (Organization `AGENTS.md`, trusted-process review Git historie Stewardem) a provider-enforced se stává až branch protection bez bypassu na placeném plánu. Principy jsou totožné, liší se jen vynucení. |
| 0149 | **Přístup Teamu k repozitářům drží živé GitHub granty.** Jedinou živou autoritou přístupu Team → moduly/repozitáře je aktuální GitHub Team membership a repository grant. Broker při každém vydání tokenu ověří neměnný GitHub Team i grant k přesnému repozitáři a při divergenci fail closed; odvolání na GitHubu tak blokuje další token. Dashboard smí vlastnit chtěné (desired) mapování Team → repozitáře jako reviewovaný vstup pro broker policy, ale mapování odvozené z Dashboardu samo přístup nikdy neuděluje ani neudržuje a nestává se druhým ACL. |
| 0150 | Greenfield klientská Organizace dokončí GitHub access baseline před první Builder instalací: `Lazurio for GitHub` používá `All repositories`, Organization base repository permission je `none`, obecnou Builder roli vlastní GitHub Team `builders`, jehož úplný paginated roster přesně odpovídá schváleným členům, a Team má explicitní WRITE na canonical rootu a všech aktivních non-restricted repech; Team ani non-admin Builders nemají grant na `infra` ani jiný `restricted`/`private` slot. Nový běžný repo grant vzniká ve stejném provisioning kroku jako aktivace slotu, protože GitHub neumí trvalé „všechny budoucí kromě infra“. Owner před instalací provede live read-back proti versioned manifestu přes runtime-owned normalizovaný slot inventory; status inventory je uzavřený na `active` / `planned_slot` / `unknown` a `unknown`, neřetězcový status nebo rozpor statusu s přítomností repository, incomplete/malformed provider data či extra/missing Builder blokují instalaci. Efektivní restricted přístup Organization ownera se eviduje jako `owner_role_exception`, protože GitHub collaborators API neumí odlišit owner autoritu od repository grantu, a nikdy se nevydává za non-admin Builder grant ani důkaz jeho provenience. GitHub zůstává jedinou access autoritou a provider zápisy dál vyžadují explicitní mandát Principála. |
| 0151 | Výchozí krátký prompt pro novou Builder Mašinu je end-to-end a console-free: pro aktuální instalační relaci výslovně autorizuje oficiální instalaci chybějících povinných nástrojů, podporovaný standardní OS package manager, standardní elevation a nejmenší nutnou změnu User i Machine/system-wide `PATH`; neautorizuje upgrade již vyhovujících nástrojů ani obecný machine-admin mandát. Task Agent provádí příkazy background nástroji a nikdy nepředává člověku práci v Terminalu nebo PowerShellu; přípustným lidským krokem zůstává pouze nativní consent, GitHub web autorizace a případný GUI relaunch Codexu, pokud jej harness neumí bezpečně provést sám. GitHub pairing používá `gh auth login --hostname github.com --git-protocol ssh --web` bez `--clipboard`: krátkodobý user-facing ověřovací (device) kód a `https://github.com/login/device` smí Agent zobrazit pouze v aktuálním soukromém chatu, zatímco interní OAuth `device_code`, access token a privátní klíč nikdy nezobrazí ani nezapíše. |
| 0152 | Explicitní `lazurio organization install` rozšiřuje bounded bootstrap z decision 0138 na aktivní deklarovaný `repository_db_mount` pouze na `mission-control/db` nebo kanonickém `workspace/<module>/db` pod právě jedním aktivním a skutečně materializovaným parent Git repozitářem; deprecated `modules/<module>/db` se nematerializuje. Root-level Mission Control si zachovává povinnou úplnost a installer-managed parent. Child remote musí patřit stejné GitHub Organization, branch i fyzická cesta musí přesně odpovídat manifestu a ignore se dokazuje v parent repozitáři. Role-scoped instalace dědí restricted hranici parenta i child slotu; inactive mount se neklonuje. Existující checkout se pouze ověří a obecný `lazurio update`, ongoing sync, commit i publish dál autoritu nezískávají. Tato část superseduje omezení 0138 pouze pro uvedené dvě podporované cesty. |
| 0153 | **Jedna osobní hostovaná Mašina per Principál.** Personalspace Principála nese právě jednu hostovanou Mašinu (osobní VM). Má-li Principál Buddyho, bydlí na téže jediné Mašině: není to druhý host ani výjimka z počtu, jen runtime uvnitř stejné hranice, jehož provoz se dál řídí `manual/hosted-buddy-vps.md` — dedikovaná per-owner VPS z decision 0080 je právě tato Mašina. Buddy zůstává volitelný a vyhrazený lidskému Principálovi (decision 0089); existence osobní Mašiny ho nezakládá ani neodvozuje pro seat AI Kolegy. Vlastní hardware Principála stojí mimo pool a jméno v `lazurio.io` nepotřebuje — je to klient. Důvod je koordinační, ne kapacitní: personalspace je gitignored a mimo GitHub organizace, takže pro víc hostovaných Mašin nemá čím koordinovat vlastnictví, jména ani přístupy, a vymýšlet mu vlastní registr by duplikovalo standardní capability. Druhá trvalá Mašina proto patří do Organizace, kde ji koordinuje GitHub; Dočasná potřeba se řeší uvnitř vlastní Mašiny — snapshotem nebo kontejnerovou či procesní izolací, které podle root `AGENTS.md` Mašinou samy nejsou. Nested guest ani krátkodobá VM výjimkou nejsou: virtuální server je Mašina i bez jména v `lazurio.io`, takže spadá pod stejné pravidlo a nesmí se stát druhým osobním domovem. Důsledek pro jména: osobní hranice = jedna Mašina = jeden hostname, takže personalspace nepotřebuje registr labelů VM ani vstup do Deployment Repa a Conglomerate Host nečte osobní repo vlastníka. Zpřesňuje „Vlastní mašina, vlastní Personalspace“ v root `AGENTS.md` a decision 0091. |
| 0154 | **Osobní hostovaná Mašina je dosažitelná jen přes Headscale Conglomerate Hostu.** Osobní VM (decision 0153) je node téhož Headscale tailnetu Conglomerate Hostu, ve kterém Principál pracuje s Organizacemi, registrovaný pod jeho osobním Headscale userem — ne pod organizačním userem ani `tagged-devices`, aby osobní Mašina zůstala odlišitelná od organizačních. Laptop i telefon Principála tak potřebují jediného klienta Tailscale; SSH, Codex i aplikace jdou tou jednou cestou. Tailscale prochází NAT hostitele sám, takže osobní VM nemá veřejný port ani DNAT a pool nealokuje žádné per-VM síťové prostředky. Servisní přístup operátora poolu zůstává oddělený od vlastnického (jmenovitý klíč přes hostitele jako jump) a nezávisí na zařízení Ownera. **Historie:** pilot DEV-6614 2026-09-20 použil pro první osobní VM per-VM WireGuard (vlastní listener v každé VM, dedikovaný veřejný UDP port s DNAT na hostiteli). Funkční průchod laptop → SSH → Codex tím byl prokázán, ale 2026-09-21 byl WireGuard zrušen a odstraněn z VM, hostitele i custody: dva VPN klienti na počítači a telefonu jsou produktově neobhajitelní a tailnet už existuje. WireGuard ani jiný druhý VPN se pro osobní Mašiny nezavádí; tento záznam je jediné místo, kde pilot zůstává jako reference. Zpřesňuje decisions 0144, 0146 a 0153. |
| 0155 | **Přístupy mezi Mašinami jednoho Operátora: dvě zóny a infra jako blueprint.** Mašiny a klienti jednoho Principála tvoří osobní zónu (osobní laptop, telefon, jediná osobní VM) a pracovní zónu (pracovní VM přiřazené právě jemu, i v několika Organizacích téhož Conglomerate, a pracovní laptop Organizace). Uvnitř zóny SSH oběma směry (pracovní VM mezi sebou ale grant nemají, ani v jedné Organizaci, ani napříč), z osobní do pracovní ano, z pracovní do osobní nikdy; sdílená týmová VM přijímá jen příchozí od členů živého GitHub Teamu (0147/0149) a nemá odchozí granty; vše platí uvnitř jednoho Conglomerate a osobní VM je uzlem jen domovského tailnetu. Správu Conglomerate Hostu má Owner Organizace, která host vlastní, jen z osobního laptopu a osobní VM. Vztahy `owner`/`operator` se váží na neměnné GitHub ID; zmrazený lowercase GitHub login je jen síťový a DNS slug (osobní Headscale user, technické jméno osobní VM), personalspace zůstává `<login>_GEN3`; existující uzly pod jiným userem se kvůli tomu znovu nepárují, přesunou se až při dalším přihlášení zařízení; zobrazované jméno (Friday, Henry) je jen popis. `<Org>/infra` vlastníka Conglomerate Hostu je čitelný blueprint: autoruje se seznam Mašin (`owner`, `operator`, zóna) a pravidla, exaktní per-Mašina granty se generují; cílem je síťová lane, kde merge do `main` síť aplikuje sám (mechanismus vybere shaping). Každý Conglomerate Host dostane vlastní IPv4 prefix v `100.64.0.0/10` (pomůcka operátora, ne centrální registr). Plné znění: `manual/operator-machine-access.md`. Zpřesňuje 0144, 0146, 0153 a 0154; realizace DEV-6614. |
| 0156 | **„AI Kolega“ je deprecated; nástupce je automatizovaná Mašina s personou (směr, shaping otevřený).** Principál 2026-09-21 rozhodl, že pojem **AI Kolega** jako AI Principál s vlastním seatem se opouští a automatizovanou práci pro Organizaci ponese **automatizovaná Mašina** Organizace, ne další Principál. **Persona** je jméno a charakter Agentů na konkrétní Mašině (osobní VM: persona Buddyho, např. Matoušova „Friday“; pracovní Mašiny: persona Organizace, např. „Henry“, asistent Macano-Tech); persona není Principál, identita ani pravomoc; na Mašině s Operátorem jednají Agenti za něj v jeho právech, u bezobslužné Mašiny to určí plné rozhodnutí. Než se slovník změní, doplní plné rozhodnutí: co přesně je automatizovaná Mašina a kdo je její Owner, GitHub identita (navazuje na 0147), nástupce role Opatrovníka, dopad na 0089–0094 a na profilovou osu LazurioPlatform (`purpose`), a persona jako vrstva profilu definovaná Organizací nebo Principálem. Pak proběhne jeden řízený sweep (`AGENTS.md`, `ARCHITECTURE.md`, GLOSSARY, šablony, LazurioPlatform), ne postupné úpravy. Do té doby existující seaty a GitHub účty (např. Steward review seat) fungují beze změny a nové texty pojem AI Kolega nezavádějí. |
| 0157 | **Vstup do osobní VM stejnou branou jako do pracovní VM; výjimka pro nativní párování T3.** Principál 2026-09-22 rozhodl: hostovaná osobní VM (0153/0154) používá stejnou stock oauth2-proxy bránu jako pracovní VM, jednu pro Launchpad, T3 Code i všechny aplikace. Admission je ID-token `groups` z client-bound mapperu `lazurio-personal-owner`, který skupinu vydá jen pro neměnné GitHub ID Ownera; RP session se obnovuje nejpozději každé 2 minuty (`cookie_refresh = 2m`, nezkrácené cookie) a neúspěšný refresh relaci ukončí, takže odpojení nebo změna GitHub vazby platí nejpozději do 2 minut. Starší návrh introspekce při každém požadavku (`manual/security/hosted-personal-launchpad.md`) je pro osobní VM nahrazený. Tím se na osobní VM rozšiřuje výjimka ID-token admission pracovních VM a pro bránu osobní VM se nahrazuje požadavek introspekce při každém požadavku (introspekční cesta zůstává pro resource servery). **Výjimka:** jen na hostname aplikace `t3code` smí nativní klient T3 bez přihlášení přes bránu použít přesně tyto cesty a metody: `GET /.well-known/t3/environment`, `POST /oauth/token`, `GET /api/auth/session`, `POST /api/auth/websocket-ticket` a websocket `GET /ws` pouze s parametrem `wsTicket`. Jakákoli jiná cesta nebo metoda na `t3code` i na všech ostatních hostnamech zůstává za bránou; výjimka se nesmí rozšířit bez nového rozhodnutí. Chrání je vlastní párovací a session autorizace T3 a tailnet pravidla. Je to vědomá odchylka od „jedné admission brány na Mašinu“, platí pro pracovní i osobní VM a je vedená jako otevřený problém `HumanAndMachine-ai/Machines#184` k případné korekci. **Přijatý důsledek:** wildcard certifikát `*.<login>.lazurio.io` zapisuje login do veřejných Certificate Transparency logů; lidem se to řekne při onboardingu. Realizace: Auth #14, Machines (osobní VM a výjimka T3), DEV-6614. |
| 0158 | **Zařízení Ownera smí být v obou zónách.** Zakladatel je týž člověk jako Owner Organizace a jeden notebook mu slouží osobně i pracovně. Klientské zařízení proto smí být deklarované v osobní i pracovní zóně; Principál, který je sám Ownerem, tím pro jedno své zařízení vědomě vzdává ochranu pravidla „z pracovní do osobní nikdy“. Platí to jen když je neměnné GitHub ID jeho vlastníka zároveň Ownerem jmenované Organizace, dvojí zařazení je v infra výslovně zapsané (nikdy odvozené ze jména, popisu, Headscale usera ani z role) a grant dostanou jen pracovní Mašiny těch Organizací, které jsou v deklaraci jmenované. Vědomým důsledkem je technická cesta autority Organization a Conglomerate Hostu do zařízení, na kterém Principál pracuje i soukromě; tu autoritu mají všichni Owneři té Organizace a každý, kdo smí spouštět práci na jejích pracovních VM, včetně agentních účtů, proto výjimka patří jen Ownerovi a jen z jeho vlastního rozhodnutí. Osobní VM výjimku nedostává, zůstává jen v osobní zóně (0153) a pracovní VM na ni nikdy nedosáhne. Dokud Machines neumí dvojí zařazení výslovně deklarovat, platí přísné pravidlo i pro zakladatelovo zařízení. Zpřesňuje 0155; realizace DEV-6614. |
| 0159 | **Dočasně volné GitHub přihlášení na hostovaných pracovních VM.** Principál 2026-09-25 rozhodl: dokud vazbu Mašina → GitHub účet neřídí Lazurio Account v Dashboardu, přihlásí operátor hostované pracovní VM — osobní i týmové — `gh` libovolným GitHub účtem. `owner.assignment` v `lazurio.machine.json` přihlášení neomezuje; Launchpad ani Agent kvůli chybějícímu, neplatnému nebo odlišnému přiřazení neblokují a Launchpad nabízí Odhlásit pro návrat jakéhokoli přihlášení. Na týmové VM bez nasazeného bota je to dočasná výjimka z 0147: všichni na VM pracují pod přihlášeným účtem; kde broker Organizace běží, zůstává jedinou identitou bot. Cíl: Owner/Admin přepne vazbu Mašina → účet v Dashboardu a Launchpad přihlášený Lazurio Accountem podle ní řídí `gh` bez Machines rolloutu; `provision.assignment` se kvůli přihlášení do té doby nedoplňuje. GitHub zůstává jedinou autoritou přístupů. |
| 0160 | **Hostovaná Mašina je pro uživatele „Remote Environment“ (Vzdálené prostředí), tento počítač „Local Environment“ (Místní prostředí).** Principál 2026-09-26: stejný pojem, jaký pro vzdálené pracovní prostředí používají Codex a T3 Code. Osa Prostředí / Environments v Launchpadu a Dashboardu nabízí obojí; „VM“ zůstává přívlastek; interní pojem je Mašina; adresy se nemění: pracovní VM `launchpad.<mašina>.<org>.lazurio.io` v síti Organizace (0146), osobní VM `launchpad.<login>.lazurio.io` v domovském tailnetu Principála (0153). Local Environment je zařízení, u kterého uživatel sedí a které není hostovanou Mašinou; listener není klasifikátor. Osobní laptop nikdy není Remote Environment. Zapsáno v LazurioPlatform jako F16 naming a v generovaném slovníku Folderu; 0159 (volné GitHub přihlášení) je samostatné rozhodnutí téhož dne. |
| 0161 | **Remote Environment: baseline provozovatele versus nástroje operátora; rollout je oprava, ne brzda.** Principál 2026-09-26 („nechci operátory zbytečně blokovat“; „nevidím pointu v pinování verzí“): (1) **Baseline provozovatele** drží pin přes rollout Machines a musí existovat, i když si operátor všechno rozbije: systém, účty a sudo, síť a Headscale, brána s certifikáty a admission, resident, Platforma (s vlastním floorem a rollbackem, decision F13/F14), Lazurio Folder s návody a **záchranný runtime pro agenta** (Codex nebo Claude Code, `gh`, `git`, Node ve známé dobré verzi) v cestě, kterou vlastní instalační autorita Mašiny (root na Linuxu, instalující admin na macOS), mimo PATH operátora; používá ho jen vstup do prostředí (`lazurio`, spouštěč T3), když nástroje operátora nefungují. (2) **Nástroje operátora jsou volné:** Codex, Claude Code, `gh`, Node, npm, Bun a další na jeho PATH a v jeho home. Baseline je dodá při vzniku Mašiny, dál si je operátor aktualizuje sám oficiálními instalátory po svém; role nikdy nedowngraduje ani nepřepisuje; readback hlásí jejich verze jako fakt, ne jako drift. Chyba Machines 0.12.83, která operátorovi odmítla vlastní `npm`, se nesmí opakovat. (3) **Rollout je oprava, ne brzda:** když si operátor prostředí rozbije tak, že agenti nefungují, re-pin obnoví jen vrstvu (1) a spustí v ní agenta ze záchranného runtimu; ten podle návodů Folderu (`manual/troubleshooting.md`) opraví vrstvu (2). Rollout se nikdy nedotýká toho, co operátor nainstaloval. (4) **T3 Code** je součást vstupu do prostředí (za bránou, v jednotce), patří do vrstvy (1) s vlastním runtimem spravovaným launcherem a opravovaným applyem, s tlačítkem Update z releasů forku `Lazurio/t3code`; T3 vždy spouští nástroje z PATH operátora; recovery jde přes `lazurio` ze záchranného runtimu, ne přes PATH fallback (DEV-6624). (5) **Pravidlo pro agenty:** agent aktualizuje nástroje operátora jen na výslovný pokyn Principála v threadu; jinak jen hlásí dostupné verze. Platformu aktualizuje výhradně `lazurio update`; nástroje operátora `lazurio tools status|update` jako tenká orchestrace oficiálních instalátorů. Nahrazuje pravidlo, že pin provozovatele Machines vybírá „nástroje a jejich aktualizace“ (0144 zůstává pro produkt a Folder). Realizace: Machines (rozdělení pinů, záchranný toolchain, readback verzí, T3 launcher #220), LazurioPlatform F17 (manuál Folderu, `lazurio tools`). |

## Workstation toolchain: PATH compatibility (2026-09-04)

Principál schválil instalaci nezávislou na správci balíčků. Pro localhost
workstation rozvíjí decision 0140 společný resolver v
`lazurio/core/toolchain-lib.mjs`: první executable z PATH procesu je autorita
výběru, jeho funkčnost a kompatibilní verze jsou readiness podmínkou. Pevné
instalační allowlisty nejsou důkaz provenance a nahrazují se tímto kontraktem
v Install Core, Doctoru i skutečných Git/GitHub consumerech. Homebrew není
požadovaná závislost ani důvod odmítnutí existující instalace. Instalátor
nejprve využije vyhovující nástroje. Nejnovější release zůstává advisory lane
0135, Bun drží exact 0134 pin z `lazurio/package.json#packageManager`. Codex má zatím capability pouze
rozpoznatelného stabilního CLI, nikoli vymyšlené minimum odvozené z latest.

Výběr prostředí patří Principálovi; shim může podle cwd měnit skutečný runtime,
takže cesta ani shodná verze neprokazují identitu binárky. Consumer testy ověřují
použití vybraného příkazu a zachování PATH pro shimy. Git context/config
sanitizace, GitHub host, explicitní argv, timeouty, access a immutable hosted
Resident/Buddy artefaktové piny zůstávají samostatnými hranicemi.
